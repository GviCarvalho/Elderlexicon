package com.elderlexicon.mod.ligabis;

/** What a Ligabis link binds between its ends. */
public enum Aspect {
    /** Integrity: health, durability, existence. */
    FIRMO,
    /** Heat. */
    IGNI,
    /** Breath. */
    AQUA,
    /** Movement. */
    AURA,
    /**
     * A bond of reading, with no bodily effect: the spirit can read the scrolls carrying the bound mark from
     * anywhere in the dimension (see {@link ReadingBond}).
     */
    VIS
}
