package com.elderlexicon.mod.spell.action;

/**
 * Categorizes each spell action so other systems can quickly branch logic.
 */
public enum SpellActionType {
    SOURCE,
    FUNCTION,
    SHAPE,
    SYSTEM
}
